import { Request, Response } from "express";
import { userSchema, IMongoUser, IUser } from "../models/user.model";
import mongoose from "mongoose";


export const UserController = {
    async create(req: Request, res: Response) {
        try {

        } catch (error) {

        }
    }
}