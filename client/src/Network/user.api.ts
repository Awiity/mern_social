// Backend For Frontend (BFF)

/*
import useFetch from "../Hooks/useFetch";

interface IUser {
    password: string,
    username: string,
    firstname: string,
    lastname: string | "",
    email: string,
    description: string | "",
    address: string | "",
    role: string | "user",
}

export async function create(user: IUser) {
    const { data, isLoading, error, reload } = useFetch("localhost:4000/api/users")
}*/